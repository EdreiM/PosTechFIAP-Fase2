# Dependências do projeto (equivalente ao environment.yml, para venv/pip puro)
# Instalação: pip install -r requirements.txt
matplotlib>=3.9
numpy>=2.0
pygame>=2.6
groq>=0.9
python-dotenv>=1.0
pytest>=8.0

